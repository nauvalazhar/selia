# Toggle

A two-state button that can be toggled on or off.

<DocsButtons externalDoc="https://base-ui.com/react/components/toggle" />

<Preview name="basic" />


## Installation
<Installation name="toggle" />

## Usage

### Import

Import the component and its related parts.

```ts filename="Toggle Parts"
import { Toggle } from "components/selia/toggle";
```

### Structure

The expected structure and composition of the component.

```tsx filename="Anatomy"
<Toggle />
```

### Props

Each component part, its responsibility, and the props it exposes.

<ComponentTable
  components={{
    Toggle: {
      description: "The toggle component.",
      source: {
        label: 'Base UI Toggle',
        href: 'https://base-ui.com/react/components/toggle',
      },
      required: true,
      props: {
        variant: {
          name: "Variant",
          options: ["default", "plain"],
          default: "default"
        },
        size: {
          name: "Size",
          options: ["sm", "md", "sm-icon", "md-icon"],
          default: "md",
        },
      }
    }
  }}
/>
