
# Separator

Dividing element to create visual distinction between content groups.

<DocsButtons externalDoc="https://base-ui.com/react/components/separator" />

<Preview name="basic" />

## Installation
<Installation name="separator" />

## Usage

### Import

Import the component and its related parts.

```ts filename="Separator Parts"
import { Separator} from "components/selia/separator";
```

### Structure

The expected structure and composition of the component.

```tsx filename="Anatomy"
<Separator />
```

### Props

Each component part, its responsibility, and the props it exposes.

<ComponentTable
  components={{
    Separator: {
      source: {
        label: 'Base UI Separator',
        href: 'https://base-ui.com/react/components/separator'
      },
      required: true,
      props: {
        orientation: {
          name: "Orientation",
          options: [
            "horizontal",
            "vertical"
          ],
          default: "horizontal",
        },
      },
    },
  }}
/>

## Examples

### Vertical

To create a vertical separator, pass the `orientation` prop to the `Separator` component.

<Preview name="vertical" />