
# Kbd

Display keyboard shortcuts and commands in a distinctive style.

<DocsButtons />

<Preview name="basic" />
## Usage

### Import

Import the component and its related parts.

```ts filename="Kbd Parts"
import {
  Kbd,
  KbdGroup
} from "components/selia/kbd";
```

### Structure

The expected structure and composition of the component.

```tsx filename="Anatomy"
<Kbd>Ctrl+C</Kbd>
```

### Props

Each component part, its responsibility, and the props it exposes.

<ComponentTable
  components={{
    Kbd: {
      type: "kbd",
      required: true,
      props: {
        variant: {
          name: "Variant",
          options: ["default", "outline", "plain"],
          default: "default",
        },
      },
    },
  }}
/>