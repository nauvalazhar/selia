
# Heading

Hierarchical text elements to establish content structure and importance.

<DocsButtons />

<Preview name="basic" />

## Installation
<Installation name="heading" />

## Usage

### Import

Import the component and its related parts.

```ts filename="Heading Parts"
import { Heading } from "components/selia/heading";
```

### Structure

The expected structure and composition of the component.

```tsx filename="Anatomy"
<Heading>Heading</Heading>
```

### Props

Each component part, its responsibility, and the props it exposes.

<ComponentTable
  components={{
    Heading: {
      type: "h1",
      required: true,
      props: {
        size: {
          name: "Size",
          options: ["sm", "md", "lg"],
          default: "md",
        },
        level: {
          name: "Level",
          options: [1, 2, 3, 4, 5, 6],
          default: 1,
        },
      },
    },
  }}
/>