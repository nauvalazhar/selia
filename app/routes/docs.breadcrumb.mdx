# Breadcrumb

Displays the path to the current resource using a hierarchy of links.

<DocsButtons />

<Preview name="basic" />

## Installation

<Installation name="breadcrumb" />

## Usage

### Import

Import the component and its related parts.

```ts filename="Breadcrumb Parts"
import {
  Breadcrumb,
  BreadcrumbList,
  BreadcrumbItem,
  BreadcrumbButton,
  BreadcrumbSeparator,
  BreadcrumbEllipsis,
} from 'components/selia/breadcrumb';
```

### Structure

The expected structure and composition of the component.

```tsx filename="Anatomy"
<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbButton>Home</BreadcrumbButton>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbButton active>Page</BreadcrumbButton>
    </BreadcrumbItem>
  </BreadcrumbList>
</Breadcrumb>
```

### Props

Each component part, its responsibility, and the props it exposes.

<ComponentTable
  components={{
    BreadcrumbButton: {
      type: 'button',
      required: true,
      props: {
        active: {
          name: 'Active',
          options: ['true', 'false'],
          default: 'false',
        },
      },
    },
  }}
/>

### Ellipsis

Use the `BreadcrumbEllipsis` component to show collapsed items.

<Preview name="ellipsis" />

### Ellipsis Menu

Compose the `BreadcrumbEllipsis` component with the `Menu` component to show a menu of collapsed items.

<Preview name="ellipsisMenu" />
