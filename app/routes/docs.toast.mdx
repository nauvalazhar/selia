
# Toast

Brief notification messages that appear temporarily at screen top.

<Preview name="basic" />
## Anatomy

Import the component and assemble its parts:

```tsx filename="Anatomy"
<Toast />
```

## Usage

### Variants

Toast comes in a variety of variants, each with its own color and style.

<Preview name="variant" />
### Promise

Toast can also be used to display a promise.

<Preview name="promise" />
### Item

Toast can be used to display a custom component.
In this example, we are using the `Item` component to display a custom toast.

<Preview name="item" />