
# Label

Text descriptions that identify and provide context for form controls.

<DocsButtons />

<Preview name="basic" />
## Guidelines

While you can use the `Field` component to group inputs and labels, the `Label` component is still useful for labeling checkboxes, radio buttons, or switches.


<Installation name="label" />

## Usage

### Import

Import the component and its related parts.

```ts filename="Label Parts"
import { Label } from "components/selia/label";
```

### Structure

The expected structure and composition of the component.

```tsx filename="Anatomy"
<Label>Label</Label>
```