
# Input

Single-line text field for user data entry and form submission.

<DocsButtons externalDoc="https://base-ui.com/react/components/input" />

<Preview name="basic" />


## Installation
<Installation name="input" />

## Usage

### Import

Import the component and its related parts.

```ts filename="Input Parts"
import { Input } from "components/selia/input";
```

### Structure

The expected structure and composition of the component.

```tsx filename="Anatomy"
<Input />
```

### Props

Each component part, its responsibility, and the props it exposes.

<ComponentTable
  components={{
    Input: {
      required: true,
      source: {
        label: 'Base UI Input',
        href: "https://base-ui.com/react/components/input"
      },
      props: {
        variant: {
          name: "Variant",
          options: [
            "default",
            "subtle",
          ],
          default: "default",
        },
      },
    },
  }}
/>

## Examples

### Subtle

Use the `variant="subtle"` prop to make the input have a subtle background color.

<Preview name="subtle" />
### File

Use the `type="file"` prop to make the input a file input.

<Preview name="file" />
### Disable

Use the `disabled` prop to make the input disabled.

<Preview name="disable" />