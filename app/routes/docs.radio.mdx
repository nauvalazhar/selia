
# Radio

Allow users to select a single option from a set of choices.

<DocsButtons externalDoc="https://base-ui.com/react/components/radio" />

<Preview name="basic" />


## Installation
<Installation name="radio" />

## Usage

### Import

Import the component and its related parts.

```ts filename="Radio Parts"
import {
  RadioGroup,
  RadioGroupLabel,
  Radio
} from "components/selia/radio";
```

### Structure

The expected structure and composition of the component.

```tsx filename="Anatomy"
<RadioGroup>
  <RadioGroupLabel>Label</RadioGroupLabel>
  <Radio />
</RadioGroup>
```

## Examples

### Item

You can use the `Item` component to create a radio item. Use the `render={<label />}` prop to make the `Item` act as a label.

<Preview name="item" />