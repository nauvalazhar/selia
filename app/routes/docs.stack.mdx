
# Stack

Layout component for arranging elements vertically or horizontally with spacing.

<DocsButtons />

<Preview name="basic" />
## Usage

### Import

Import the component and its related parts.

```ts filename="Stack Parts"
import { Stack } from "components/selia/stack";
```

### Structure

The expected structure and composition of the component.

```tsx filename="Anatomy"
<Stack>
  <Item />
</Stack>
```

### Props

Each component part, its responsibility, and the props it exposes.

<ComponentTable
  components={{
    Stack: {
      description: "The stack container.",
      required: true,
      props: {
        direction: {
          name: "Direction",
          options: ["row", "column"],
          default: "column",
        },
        spacing: {
          name: "Spacing",
          options: ["sm", "md", "lg"],
          default: "md",
        },
      },
    },
  }}
/>

## Examples

### Row

To create a row stack, pass the `direction="row"` prop to the `Stack` component.

<Preview name="row" />