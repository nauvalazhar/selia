# Collapsible

Show and hide content with a trigger.

<DocsButtons externalDoc="https://base-ui.com/react/components/collapsible" />

<Preview name="basic" />


## Installation
<Installation name="collapsible" />

## Usage

### Import

Import the component and its related parts.

```ts filename="Collapsible Parts"
import {
  Collapsible,
  CollapsibleTrigger,
  CollapsiblePanel
} from "components/selia/collapsible";
```

### Structure

The expected structure and composition of the component.

```tsx filename="Anatomy"
<Collapsible>
  <CollapsibleTrigger>Trigger</CollapsibleTrigger>
  <CollapsiblePanel>Collapsible content</CollapsiblePanel>
</Collapsible>
```

### Props

Each component part, its responsibility, and the props it exposes.

<ComponentTable
  components={{
    CollapsibleTrigger: {
      source: {
        label: 'Base UI Collapsible',
        href: 'https://base-ui.com/react/components/collapsible'
      },
      required: true,
      props: {
        expandableIndicator: {
          options: ["true", "false"],
          default: "true",
        },
      }
    },
  }}
/>