# Scroll Area

A native scroll container with custom scrollbars.

<DocsButtons externalDoc="https://base-ui.com/react/components/scroll-area" />

<Preview name="basic" />

## Usage

### Import

Import the component and its related parts.

```ts filename="Scroll Area Parts"
import { ScrollArea } from "components/selia/scroll-area";
```

### Structure

The expected structure and composition of the component.

```tsx filename="Anatomy"
<ScrollArea>
  Long content goes here
</ScrollArea>
```

### Props

Each component part, its responsibility, and the props it exposes.

<ComponentTable
  components={{
    ScrollArea: {
      description: "The scroll area component.",
      source: {
        label: 'Base UI Scroll Area',
        href: 'https://base-ui.com/react/components/scroll-area'
      },
      required: true,
      props: {
        scrollbar: {
          name: "Scrollbar",
          options: [
            "horizontal",
            "vertical",
            "both",
            "false"
          ],
          default: "both",
        },
      },
    },
  }}
/>